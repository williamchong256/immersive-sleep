type Data @model @auth(rules: [{allow: public}]) {
  id: ID!
  date: String!
  heartRate: String!
  breathing: String!
  efficiency: String!
  duration: String!
}

type User @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
}
 